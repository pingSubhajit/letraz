import {charter} from '@/components/resume/themes/DEFAULT_THEME/fonts'

const EducationTitle = () => (
	<div className={`${charter.className} section-header`}>Education</div>
)

export default EducationTitle
