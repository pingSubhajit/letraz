import {charter} from '@/components/resume/themes/DEFAULT_THEME/fonts'

const SkillsTitle = () => {
	return (
		<div className={`${charter.className} section-header`}>Skills</div>
	)
}

export default SkillsTitle